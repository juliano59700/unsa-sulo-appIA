---
phase: 04-basic-transcription
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/server.ts]
autonomous: false

must_haves:
  truths:
    - "Both transcription tools appear in MCP tool list"
    - "Users can call both tools through MCP protocol"
    - "End-to-end transcription workflow with automatic polling functions correctly"
  artifacts:
    - path: "src/server.ts"
      provides: "MCP server with transcription tools registered"
      exports: ["server", "main"]
  key_links:
    - from: "src/server.ts ListToolsRequestSchema"
      to: "transcribeTool.definition, transcriptionStatusTool.definition"
      via: "tools array"
      pattern: "tools: \\[.*transcribeTool\\.definition.*transcriptionStatusTool\\.definition.*\\]"
    - from: "src/server.ts CallToolRequestSchema"
      to: "transcribeTool.handler, transcriptionStatusTool.handler"
      via: "name-based routing"
      pattern: "request\\.params\\.name === 'transcribe'|'get_transcription_status'"
---

<objective>
Register transcription tools with MCP server and verify complete transcription workflow

Purpose: Complete Phase 4 by making transcription tools available through MCP protocol and ensuring end-to-end functionality including automatic polling works correctly
Output: Fully functional transcription capability with automatic result retrieval accessible through MCP client
</objective>

<execution_context>
@/Users/jean-louisqueguiner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jean-louisqueguiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-basic-transcription/04-01-SUMMARY.md
@src/server.ts
@src/tools/upload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register transcription tools in MCP server</name>
  <files>src/server.ts</files>
  <action>
    Update src/server.ts to register both new tools following the established pattern:
    1. Import transcribeTool from ./tools/transcribe.js
    2. Import transcriptionStatusTool from ./tools/transcription_status.js
    3. Add both tool definitions to the tools array in ListToolsRequestSchema handler (alongside existing uploadTool.definition)
    4. Add routing for both tools in CallToolRequestSchema handler:
       - 'transcribe' -> transcribeTool.handler
       - 'get_transcription_status' -> transcriptionStatusTool.handler
    5. Maintain existing error handling for unknown tool names
    
    Follow the exact same pattern established for uploadTool registration.
  </action>
  <verify>npm run build succeeds and server imports both tools correctly</verify>
  <done>MCP server lists 3 tools (upload_file, transcribe, get_transcription_status) and routes all tool calls correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify transcription workflow</name>
  <what-built>Complete transcription workflow with automatic polling, job submission, and status checking tools</what-built>
  <how-to-verify>
    1. Build and test the MCP server: `npm run build && npm run dev`
    2. Verify all 3 tools are listed when MCP client connects
    3. Test upload workflow: use upload_file tool with a small audio file
    4. Test transcription: use transcribe tool with the upload URL - it should automatically poll and return completed results
    5. Test manual status checking: use get_transcription_status tool with a job ID
    6. Verify that language, diarization, and subtitle options can be configured
    7. Confirm error messages are clear and actionable for invalid inputs
    
    Expected behavior:
    - upload_file returns audio_url
    - transcribe automatically polls and returns completed transcription result
    - For long files, transcribe may timeout and return job ID for manual checking
    - get_transcription_status shows job progress/completion when called manually
    - Configuration options (language: 'en', diarization: true) are accepted
  </how-to-verify>
  <resume-signal>Type "approved" once transcription workflow is working correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- MCP server starts without errors and lists all 3 tools
- All tools can be called through MCP protocol
- Transcribe tool automatically polls and returns completed results (TRANS-03)
- Transcription jobs can be submitted with various configuration options (TRANS-02)
- Job status can be checked independently after submission (fallback)
- Error messages are clear and actionable (DX-03)
</verification>

<success_criteria>
- User can submit transcription job with language selection
- User can enable/disable diarization and subtitle generation
- User receives transcription result automatically after job completes (automatic polling)
- On timeout, user gets job ID to check manually
- Transcription errors surface with clear, actionable messages
- Complete workflow tested end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/04-basic-transcription/04-02-SUMMARY.md`
</output>
