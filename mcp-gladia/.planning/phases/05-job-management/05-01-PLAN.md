---
phase: 05-job-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/gladia.ts, src/lib/gladia.ts, src/tools/list_jobs.ts, src/tools/delete_job.ts]
autonomous: true

must_haves:
  truths:
    - "User can list all past transcription jobs with status and metadata"
    - "User can delete specific transcription jobs by ID"
    - "Job list shows duration, language, and completion status clearly"
    - "Delete operation provides clear success/failure feedback"
  artifacts:
    - path: "src/types/gladia.ts"
      provides: "Job management types and schemas"
      exports: ["ListJobsOptions", "TranscriptionListResponse", "TranscriptionJobItem"]
      min_lines: 60
    - path: "src/lib/gladia.ts"
      provides: "Job management API methods"
      exports: ["listTranscriptionJobs", "deleteTranscriptionJob"]
      min_lines: 200
    - path: "src/tools/list_jobs.ts"
      provides: "List jobs MCP tool"
      exports: ["listJobsTool"]
      min_lines: 40
    - path: "src/tools/delete_job.ts"
      provides: "Delete job MCP tool"
      exports: ["deleteJobTool"]
      min_lines: 35
  key_links:
    - from: "src/tools/list_jobs.ts"
      to: "GladiaClient.listTranscriptionJobs"
      via: "method call with filtering options"
      pattern: "client\\.listTranscriptionJobs\\("
    - from: "src/tools/delete_job.ts"
      to: "GladiaClient.deleteTranscriptionJob"
      via: "method call with jobId"
      pattern: "client\\.deleteTranscriptionJob\\("
    - from: "src/lib/gladia.ts listTranscriptionJobs"
      to: "GET /v2/transcription"
      via: "fetch with query parameters and pagination"
      pattern: "fetch.*v2/transcription.*GET"
    - from: "src/lib/gladia.ts deleteTranscriptionJob"
      to: "DELETE /v2/pre-recorded/{id}"
      via: "fetch with DELETE method"
      pattern: "fetch.*v2/pre-recorded.*DELETE"
---

<objective>
Implement job management API client methods and MCP tools for listing and deleting transcription jobs

Purpose: Enable users to monitor all their transcription jobs (TRANS-04) and delete specific jobs (TRANS-05), providing complete job lifecycle management beyond just status checking
Output: Complete job management workflow with list/delete operations accessible through MCP tools
</objective>

<execution_context>
@/Users/jean-louisqueguiner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jean-louisqueguiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-job-management/05-RESEARCH.md
@src/lib/gladia.ts
@src/types/gladia.ts
@src/tools/upload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add job management types to type definitions</name>
  <files>src/types/gladia.ts</files>
  <action>
    Add new type definitions to src/types/gladia.ts for job management:
    
    1. ListJobsOptions interface with optional fields:
       - offset?: number
       - limit?: number  
       - status?: 'queued' | 'processing' | 'done' | 'error'
       - after_date?: string (ISO 8601 format)
       - before_date?: string (ISO 8601 format)
       - kind?: 'pre-recorded' | 'live'
    
    2. TranscriptionJobItem interface for individual job metadata:
       - id: string
       - status: 'queued' | 'processing' | 'done' | 'error'
       - created_at: string
       - language?: string
       - duration?: number
       - filename?: string
    
    3. TranscriptionListResponse interface for paginated results:
       - items: TranscriptionJobItem[]
       - first: string (pagination URL)
       - current: string (pagination URL)  
       - next?: string (pagination URL, optional if last page)
       - total_count: number
    
    Follow the exact naming and structure from the research findings. These types will be used by the API client methods.
  </action>
  <verify>npm run build succeeds with no TypeScript errors on new types</verify>
  <done>Types exist for ListJobsOptions, TranscriptionJobItem, and TranscriptionListResponse with proper field types</done>
</task>

<task type="auto">
  <name>Task 2: Implement job management API client methods</name>
  <files>src/lib/gladia.ts</files>
  <action>
    Extend GladiaClient in src/lib/gladia.ts with two new methods:
    
    1. listTranscriptionJobs(options?: ListJobsOptions): Promise<TranscriptionListResponse>
       - Build URL with base path `/v2/transcription`
       - Add query parameters from options using URLSearchParams.set() for proper encoding
       - Only add parameters that are defined (filter undefined values)
       - Use GET method with x-gladia-key header
       - Handle response: 200 OK returns job list, handle errors with descriptive messages
       - Use same error handling pattern as existing methods (try/catch with GladiaErrorResponse parsing)
    
    2. deleteTranscriptionJob(jobId: string): Promise<void>
       - Use DELETE method to `/v2/pre-recorded/${jobId}` (NOT deprecated /v2/transcription/{id})
       - Include x-gladia-key header
       - Handle responses: 202 success, 404 job not found, 403 job not deletable
       - Return void on success (202 response has no body)
       - Throw specific error messages for 404 ("Transcription job {jobId} not found") and 403 ("Transcription job {jobId} is not in a deletable state")
       - Use same error handling pattern as existing methods
    
    Import new types at the top of file. Follow established patterns for fetch configuration, error handling, and response parsing.
  </action>
  <verify>npm run build succeeds and GladiaClient has both listTranscriptionJobs and deleteTranscriptionJob methods</verify>
  <done>GladiaClient contains listTranscriptionJobs and deleteTranscriptionJob methods with proper error handling and type safety</done>
</task>

<task type="auto">
  <name>Task 3: Create list jobs MCP tool</name>
  <files>src/tools/list_jobs.ts</files>
  <action>
    Create src/tools/list_jobs.ts following the established tool pattern from upload.ts:
    
    1. Zod input schema validating:
       - offset (number, optional)
       - limit (number, optional, default 20)
       - status (enum: 'queued' | 'processing' | 'done' | 'error', optional)
       - afterDate (string, optional)
       - beforeDate (string, optional) 
       - kind (enum: 'pre-recorded' | 'live', optional)
    
    2. Tool definition:
       - name: 'list_transcription_jobs'
       - description: 'List transcription jobs with optional filtering by status, date range, and type. Shows job ID, status, creation date, and metadata for monitoring transcription history.'
       - inputSchema with all optional parameters properly typed
    
    3. Handler function:
       - Parse and validate input with Zod
       - Map camelCase inputs to snake_case for API (afterDate -> after_date, beforeDate -> before_date)
       - Create GladiaClient instance
       - Call listTranscriptionJobs with mapped options
       - Return formatted job list with clear metadata display
       - Use same error handling pattern (try/catch, isError response)
    
    Set reasonable defaults: limit=20 if not specified.
  </action>
  <verify>TypeScript compiles without errors and listJobsTool export exists with definition and handler</verify>
  <done>listJobsTool filters and displays transcription jobs with status, dates, and pagination support</done>
</task>

<task type="auto">
  <name>Task 4: Create delete job MCP tool</name>
  <files>src/tools/delete_job.ts</files>
  <action>
    Create src/tools/delete_job.ts following the established tool pattern:
    
    1. Zod input schema validating:
       - jobId (string, required)
    
    2. Tool definition:
       - name: 'delete_transcription_job'  
       - description: 'Delete a transcription job by ID. This permanently removes the job and all associated data. The job must be in a deletable state (completed or failed).'
       - inputSchema with required jobId
    
    3. Handler function:
       - Parse and validate input with Zod
       - Create GladiaClient instance
       - Call deleteTranscriptionJob with jobId
       - Return success confirmation with job ID
       - Handle specific errors: 404 (job not found), 403 (not deletable state)
       - Use same error handling pattern as other tools
    
    Return clear success message: "Transcription job {jobId} deleted successfully"
  </action>
  <verify>TypeScript compiles without errors and deleteJobTool export exists with definition and handler</verify>
  <done>deleteJobTool deletes transcription jobs with clear success/failure feedback and proper error handling</done>
</task>

</tasks>

<verification>
- All new types import correctly and follow existing patterns
- GladiaClient has listTranscriptionJobs and deleteTranscriptionJob methods
- listTranscriptionJobs uses proper query parameter encoding and pagination
- deleteTranscriptionJob uses correct endpoint (/v2/pre-recorded/{id}) and handles all response codes
- Both tool files export tool objects with definition + handler
- Tools follow established MCP tool patterns and error handling
- List tool supports filtering and pagination options
- Delete tool provides clear feedback and handles edge cases
</verification>

<success_criteria>
- User can list all past transcription jobs with filtering options (TRANS-04)
- Job list shows ID, status, creation date, and metadata clearly
- User can delete specific transcription jobs by ID (TRANS-05)
- Delete operation handles "not found" and "not deletable" cases gracefully
- Both tools follow established patterns and provide clear error messages
- TypeScript compilation succeeds with full type safety
</success_criteria>

<output>
After completion, create `.planning/phases/05-job-management/05-01-SUMMARY.md`
</output>