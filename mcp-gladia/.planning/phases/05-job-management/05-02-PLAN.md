---
phase: 05-job-management
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/server.ts]
autonomous: false

must_haves:
  truths:
    - "Both job management tools appear in MCP tool list"
    - "Users can call both tools through MCP protocol"
    - "End-to-end job management workflow functions correctly"
    - "Job list displays status, metadata, and allows filtering"
    - "Job deletion provides clear success/failure feedback"
  artifacts:
    - path: "src/server.ts"
      provides: "MCP server with job management tools registered"
      exports: ["server", "main"]
  key_links:
    - from: "src/server.ts ListToolsRequestSchema"
      to: "listJobsTool.definition, deleteJobTool.definition"
      via: "tools array"
      pattern: "tools: \\[.*listJobsTool\\.definition.*deleteJobTool\\.definition.*\\]"
    - from: "src/server.ts CallToolRequestSchema"
      to: "listJobsTool.handler, deleteJobTool.handler"
      via: "name-based routing"
      pattern: "request\\.params\\.name === 'list_transcription_jobs'|'delete_transcription_job'"
---

<objective>
Register job management tools with MCP server and verify complete job management workflow

Purpose: Complete Phase 5 by making job management tools available through MCP protocol and ensuring end-to-end functionality for listing and deleting transcription jobs works correctly
Output: Fully functional job management capability with list/delete operations accessible through MCP client
</objective>

<execution_context>
@/Users/jean-louisqueguiner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jean-louisqueguiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-job-management/05-01-SUMMARY.md
@src/server.ts
@src/tools/upload.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register job management tools in MCP server</name>
  <files>src/server.ts</files>
  <action>
    Update src/server.ts to register both new job management tools following the established pattern:
    
    1. Import listJobsTool from ./tools/list_jobs.js
    2. Import deleteJobTool from ./tools/delete_job.js  
    3. Add both tool definitions to the tools array in ListToolsRequestSchema handler (alongside existing uploadTool.definition, transcribeTool.definition, transcriptionStatusTool.definition)
    4. Add routing for both tools in CallToolRequestSchema handler:
       - 'list_transcription_jobs' -> listJobsTool.handler
       - 'delete_transcription_job' -> deleteJobTool.handler
    5. Maintain existing error handling for unknown tool names
    
    The server should now list 5 tools total: upload_file, transcribe, get_transcription_status, list_transcription_jobs, delete_transcription_job
    
    Follow the exact same pattern established for existing tool registration.
  </action>
  <verify>npm run build succeeds and server imports both job management tools correctly</verify>
  <done>MCP server lists 5 tools and routes all tool calls correctly including job management tools</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify job management workflow</name>
  <what-built>Complete job management workflow with list and delete operations for transcription jobs, integrated with existing transcription workflow</what-built>
  <how-to-verify>
    1. Build and test the MCP server: `npm run build && npm run dev`
    2. Verify all 5 tools are listed when MCP client connects:
       - upload_file
       - transcribe  
       - get_transcription_status
       - list_transcription_jobs
       - delete_transcription_job
    3. Test complete workflow:
       a. Use upload_file tool with a small audio file
       b. Use transcribe tool with the upload URL to create a transcription job
       c. Use list_transcription_jobs tool to see all jobs (should include the new job)
       d. Use list_transcription_jobs with filtering (e.g., status: 'done' or limit: 5)
       e. Use delete_transcription_job tool with a completed job ID
       f. Use list_transcription_jobs again to verify the job was deleted
    4. Test error cases:
       - delete_transcription_job with invalid job ID (should return 404 error)
       - list_transcription_jobs with invalid parameters (should validate inputs)
    5. Verify job list display:
       - Shows job ID, status, creation date clearly
       - Includes metadata like language, duration when available
       - Pagination information (total_count, next URL if applicable)
    
    Expected behavior:
    - list_transcription_jobs returns paginated list with job metadata
    - Filtering options work correctly (status, date range, limit)
    - delete_transcription_job removes job and confirms deletion
    - Error messages are clear for invalid operations
    - All tools integrate smoothly with existing transcription workflow
  </how-to-verify>
  <resume-signal>Type "approved" once job management workflow is working correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- MCP server starts without errors and lists all 5 tools
- All job management tools can be called through MCP protocol
- List tool displays transcription jobs with proper metadata and filtering (TRANS-04)
- Delete tool removes jobs and provides clear feedback (TRANS-05)
- Job management integrates seamlessly with existing transcription workflow
- Error messages are clear and actionable
- TRANS-06 (status checking) remains available via existing get_transcription_status tool
</verification>

<success_criteria>
- User can list all past transcription jobs with status and metadata
- User can delete specific transcription job by ID  
- User can check status of running transcription job (via existing tool)
- Job list shows duration, language, and completion status clearly
- Complete job lifecycle management tested end-to-end
- All requirements TRANS-04, TRANS-05, TRANS-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-job-management/05-02-SUMMARY.md`
</output>