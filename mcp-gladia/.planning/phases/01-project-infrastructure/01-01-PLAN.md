---
phase: 01-project-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsup.config.ts, .gitignore, src/config/env.ts, src/server.ts, src/index.ts]
autonomous: true

must_haves:
  truths:
    - "Package has proper npm configuration for CLI installation"
    - "Environment validation rejects missing/invalid API keys"
    - "MCP server initializes with stdio transport"
    - "CLI entry point connects to MCP server properly"
  artifacts:
    - path: "package.json"
      provides: "npm package configuration with bin entry"
      contains: '"type": "module"'
    - path: "src/config/env.ts" 
      provides: "Zod environment validation"
      exports: ["env", "Environment"]
    - path: "src/server.ts"
      provides: "MCP server implementation"
      exports: ["server"]
    - path: "src/index.ts"
      provides: "CLI entry point"
      contains: "#!/usr/bin/env node"
    - path: "tsup.config.ts"
      provides: "Build configuration with shebang injection"
      contains: "banner"
  key_links:
    - from: "package.json"
      to: "dist/index.js"
      via: "bin entry"
      pattern: '"mcp-gladia".*"./dist/index.js"'
    - from: "src/index.ts"
      to: "src/server.ts"
      via: "import"
      pattern: 'import.*"./server.js"'
    - from: "src/server.ts"
      to: "src/config/env.ts"
      via: "import"
      pattern: 'import.*env.*"./config/env.js"'
---

<objective>
Create the foundational TypeScript MCP server package with proper npm configuration and environment validation.

Purpose: Establish the core infrastructure for an installable, executable MCP server that validates API keys and implements stdio transport per MCP standards.
Output: Complete package structure with configuration, environment validation, and MCP server implementation ready for building.
</objective>

<execution_context>
@/Users/jean-louisqueguiner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jean-louisqueguiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json and build configuration</name>
  <files>package.json, tsup.config.ts, .gitignore</files>
  <action>
    Create package.json as ESM-only npm package with proper CLI bin entry pointing to dist/index.js. Include @modelcontextprotocol/sdk v1.26.0, zod for validation, and dev dependencies (typescript 5.3+, @types/node 20.10+, tsup 8.0+, tsx 4.6+). Set "type": "module", engines node >=18, files array ["dist"], main/types pointing to dist. Include build/dev/test scripts.

    Create tsup.config.ts with ESM format, entry src/index.ts, clean/dts/sourcemap true, target node18, and banner.js with shebang "#!/usr/bin/env node" for CLI executable.

    Create .gitignore excluding node_modules, dist/, *.log, .env* files.
  </action>
  <verify>package.json exists with proper structure, tsup.config.ts has shebang banner, .gitignore excludes build artifacts</verify>
  <done>Package configuration is complete with ESM setup, MCP dependencies, and CLI bin entry configured</done>
</task>

<task type="auto">
  <name>Task 2: Implement environment validation with Zod</name>
  <files>src/config/env.ts</files>
  <action>
    Create src/config/env.ts implementing Zod schema for environment variable validation. Schema requires GLADIA_API_KEY as non-empty string, includes optional NODE_ENV enum (development|production) defaulting to production. Export validated env object and Environment type. Include clear error messages for missing/invalid API key.

    Use z.object with GLADIA_API_KEY validation .min(1, "GLADIA_API_KEY is required"). Parse process.env with schema.parse() to fail fast on missing env vars.
  </action>
  <verify>src/config/env.ts exports env object and Environment type, includes Zod validation with clear error messages</verify>
  <done>Environment validation is implemented with type-safe API key requirement and clear validation errors</done>
</task>

<task type="auto">
  <name>Task 3: Implement MCP server with stdio transport</name>
  <files>src/server.ts, src/index.ts</files>
  <action>
    Create src/server.ts implementing MCP server using @modelcontextprotocol/sdk. Import Server and StdioServerTransport, create server instance with name "mcp-gladia" and version "1.0.0", capabilities object with empty tools for now. Implement main() function connecting StdioServerTransport, add error handling with console.error logging (not console.log - stdout reserved for MCP protocol). Import and validate environment on server startup.

    Create src/index.ts as CLI entry point with shebang comment, importing and starting the server. Use .js extensions in ESM imports. Include error handling and process.exit(1) on failure.
  </action>
  <verify>src/server.ts implements MCP server with stdio transport, src/index.ts is proper CLI entry with shebang, imports use .js extensions</verify>
  <done>MCP server implements stdio transport, validates environment on startup, and has proper CLI entry point with error handling</done>
</task>

</tasks>

<verification>
- package.json has "type": "module" and proper bin entry
- tsup.config.ts includes shebang banner injection
- src/config/env.ts validates GLADIA_API_KEY with Zod
- src/server.ts implements MCP server with stdio transport
- src/index.ts is proper CLI entry with shebang
- All imports use .js extensions for ESM compatibility
</verification>

<success_criteria>
- Complete package structure following research recommendations
- Environment validation fails fast with clear errors on missing API key
- MCP server initializes with stdio transport using official SDK
- All files follow ESM-only configuration
- Package is ready for building and publishing
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-infrastructure/01-01-SUMMARY.md`
</output>