---
phase: 01-project-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [dist/index.js, dist/index.d.ts]
autonomous: false

must_haves:
  truths:
    - "npm install completes successfully"
    - "npx mcp-gladia starts the server without errors"
    - "Missing API key shows clear validation error"
    - "Valid API key allows server to start and listen on stdio"
    - "MCP client can connect via stdio transport"
  artifacts:
    - path: "dist/index.js"
      provides: "Built CLI executable with shebang"
      contains: "#!/usr/bin/env node"
    - path: "dist/index.d.ts"
      provides: "TypeScript declarations"
      min_lines: 1
    - path: "package-lock.json"
      provides: "Locked dependency versions"
      contains: "@modelcontextprotocol/sdk"
  key_links:
    - from: "npx mcp-gladia"
      to: "dist/index.js"
      via: "npm bin resolution"
      pattern: "package.json bin entry"
    - from: "dist/index.js shebang"
      to: "node executable"
      via: "filesystem execution"
      pattern: "#!/usr/bin/env node"
    - from: "server startup"
      to: "env validation"
      via: "import and parse"
      pattern: "GLADIA_API_KEY required error"
---

<objective>
Build and verify the complete npm package installation and CLI functionality with proper error handling.

Purpose: Ensure the package can be installed via npm, executed via npx, and properly validates the Gladia API key with clear error messages as required by DX-01, DX-02.
Output: Verified working CLI package that starts MCP server on stdio transport with environment validation.
</objective>

<execution_context>
@/Users/jean-louisqueguiner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jean-louisqueguiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-infrastructure/01-RESEARCH.md
@.planning/phases/01-project-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and build package</name>
  <files>package-lock.json, dist/index.js, dist/index.d.ts</files>
  <action>
    Run npm install to install all dependencies from package.json. Verify @modelcontextprotocol/sdk, zod, and dev dependencies install correctly.

    Run npm run build (tsup) to compile TypeScript to dist/ directory. Verify built dist/index.js has proper shebang header from tsup banner injection. Verify dist/index.d.ts type declarations are generated. Ensure executable permissions are set on dist/index.js.

    Check that built files follow ESM format with proper import/export syntax.
  </action>
  <verify>npm install completes without errors, npm run build produces dist/index.js with shebang and dist/index.d.ts</verify>
  <done>Package dependencies are installed and TypeScript code is built to executable CLI package in dist/</done>
</task>

<task type="auto">
  <name>Task 2: Test CLI execution and environment validation</name>
  <files></files>
  <action>
    Test the built CLI by running npx ./dist/index.js without GLADIA_API_KEY to verify environment validation fails with clear error message about missing API key.

    Create a test GLADIA_API_KEY="test_key" and run npx GLADIA_API_KEY=test_key ./dist/index.js to verify server starts with "MCP server started on stdio" message to stderr and waits for MCP protocol input.

    Verify the server process can be interrupted cleanly with Ctrl+C.
  </action>
  <verify>CLI fails gracefully without API key, starts successfully with API key, outputs proper error/success messages to stderr</verify>
  <done>Environment validation works correctly and server starts properly with valid API key</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Complete MCP server CLI package with environment validation, stdio transport, and npm installation capability</what-built>
  <how-to-verify>
    1. Run `npm pack` to create tarball and verify package contents
    2. Install locally with `npm install ./mcp-gladia-*.tgz`
    3. Test npx execution: `npx mcp-gladia` should show API key error
    4. Test with API key: `GLADIA_API_KEY=test npx mcp-gladia` should start server
    5. Verify MCP client can potentially connect (server listens on stdio)
    6. Confirm server logs only go to stderr, stdout is clear for MCP protocol
    7. Test that Ctrl+C cleanly exits the server process
  </how-to-verify>
  <resume-signal>Type "approved" if installation works and API key validation is correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- npm install and build complete successfully
- npx execution works with proper shebang
- Missing API key shows Zod validation error  
- Valid API key starts MCP server on stdio
- Server logs to stderr only, stdout reserved for MCP protocol
- Package is properly installable and executable
</verification>

<success_criteria>
- DX-01: Package installs via npm and runs via npx command ✓
- DX-02: API key configured via GLADIA_API_KEY with clear error on missing ✓
- DX-06: Server uses stdio transport compatible with MCP clients ✓  
- Server gracefully handles missing API key with clear error message ✓
- Phase 1 success criteria fully met
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-infrastructure/01-02-SUMMARY.md`
</output>