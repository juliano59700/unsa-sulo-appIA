---
phase: 03-file-upload
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/tools/upload.ts, src/server.ts]
autonomous: true

must_haves:
  truths:
    - "User can call upload_file tool with filePath parameter"
    - "Tool validates file before attempting upload to Gladia"
    - "Tool returns upload URL on success for transcription use"
    - "Tool returns clear error messages on validation or upload failures"
    - "Server lists upload_file in tools array"
  artifacts:
    - path: "src/tools/upload.ts"
      provides: "MCP tool implementation for file upload"
      exports: ["registerUploadTool"]
      min_lines: 40
    - path: "src/server.ts"
      provides: "Updated server with upload tool registration"
      contains: "upload_file"
      modified: true
  key_links:
    - from: "src/server.ts ListToolsRequestSchema"
      to: "upload_file tool"
      via: "tools array registration"
      pattern: "upload_file"
    - from: "src/server.ts CallToolRequestSchema"
      to: "src/tools/upload.ts handler"
      via: "tool name routing"
      pattern: "upload_file.*handler"
    - from: "src/tools/upload.ts"
      to: "src/lib/gladia.ts GladiaClient"
      via: "import and method call"
      pattern: "GladiaClient.*uploadFile"
---

<objective>
Create the upload_file MCP tool and integrate it with the server

Purpose: Exposes Gladia file upload functionality through MCP protocol, allowing users to upload audio/video files for transcription
Output: Working MCP tool that accepts file paths and returns Gladia upload URLs with proper error handling
</objective>

<execution_context>
@/Users/jean-louisqueguiner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jean-louisqueguiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-file-upload/03-RESEARCH.md
@.planning/phases/03-file-upload/03-01-SUMMARY.md
@src/server.ts
@src/config/env.ts
@src/lib/gladia.ts
@src/types/gladia.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Upload Tool Implementation</name>
  <files>src/tools/upload.ts</files>
  <action>
    Create MCP tool implementation for file uploads following research patterns:
    
    1. Import dependencies: Zod for validation, GladiaClient from ../lib/gladia.ts, env from ../config/env.ts, MCP types
    2. Define input schema with Zod: filePath as required string with description
    3. Export registerUploadTool function that adds both ListTools and CallTool handlers to server
    4. ListTools handler: return upload_file tool with name, description, and inputSchema
    5. CallTool handler: 
       - Validate tool name is 'upload_file'
       - Parse filePath from arguments using Zod schema
       - Create GladiaClient instance with env.GLADIA_API_KEY
       - Call client.uploadFile(filePath)
       - Return success response with upload URL formatted for user
       - Return error response with clear message on failures
    
    Follow MCP SDK patterns from existing server.ts for handler registration structure. Tool description should explain it uploads audio/video files to Gladia for transcription.
  </action>
  <verify>Tool compiles successfully and exports registerUploadTool function</verify>
  <done>upload_file MCP tool implemented with input validation and proper error handling</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Upload Tool with Server</name>
  <files>src/server.ts</files>
  <action>
    Update the MCP server to register the upload tool:
    
    1. Import registerUploadTool from './tools/upload.js' (use .js extension for ES modules)
    2. Call registerUploadTool(server) after the server is created but before the main() function
    3. Remove the empty tools array from ListToolsRequestSchema handler - tool registration will be handled by registerUploadTool
    4. Update CallToolRequestSchema handler to remove the hardcoded "unknown tool" error - tool routing will be handled by registerUploadTool
    
    Keep existing server setup, transport configuration, and error handling unchanged. Only modify the tool registration portions per the research pattern of modular tool registration.
  </action>
  <verify>Server starts successfully and npm run dev shows no TypeScript errors</verify>
  <done>Server registers upload_file tool and routes calls to upload handler properly</done>
</task>

</tasks>

<verification>
- Server lists upload_file in tools when MCP client calls list_tools
- Tool accepts filePath parameter and validates input properly
- Valid file uploads return Gladia upload URL in response
- Invalid files return clear error messages (wrong format, too large, not found)
- Gladia API errors surface with actionable messages
</verification>

<success_criteria>
- upload_file tool appears in MCP client tool list
- Tool successfully uploads valid audio/video files to Gladia
- Tool rejects invalid files with specific error messages
- Upload response contains audio_url field for transcription use
- Server maintains stable connection during upload operations
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-upload/03-02-SUMMARY.md`
</output>