---
phase: 03-file-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/gladia.ts, src/types/gladia.ts]
autonomous: true

must_haves:
  truths:
    - "Gladia API client can authenticate with x-gladia-key header"
    - "Client can upload files via multipart/form-data to /v2/upload"
    - "File validation rejects unsupported formats and oversized files"
  artifacts:
    - path: "src/lib/gladia.ts"
      provides: "HTTP client for Gladia API interactions"
      exports: ["GladiaClient"]
      min_lines: 50
    - path: "src/types/gladia.ts"
      provides: "TypeScript definitions for Gladia API responses"
      exports: ["GladiaUploadResponse"]
      min_lines: 10
  key_links:
    - from: "src/lib/gladia.ts"
      to: "https://api.gladia.io/v2/upload"
      via: "fetch with FormData body"
      pattern: "fetch.*gladia\\.io.*upload"
    - from: "src/lib/gladia.ts"
      to: "process.env.GLADIA_API_KEY"
      via: "x-gladia-key header"
      pattern: "x-gladia-key.*apiKey"
---

<objective>
Create the foundational Gladia API client and type definitions for file uploads

Purpose: Establishes the shared HTTP client pattern that all Gladia API interactions will use, with comprehensive file validation to prevent upload failures
Output: Gladia client class with upload method and TypeScript definitions for API responses
</objective>

<execution_context>
@/Users/jean-louisqueguiner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jean-louisqueguiner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-file-upload/03-RESEARCH.md
@src/config/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gladia API Type Definitions</name>
  <files>src/types/gladia.ts</files>
  <action>
    Create TypeScript interfaces for Gladia API responses. Based on research, the upload endpoint returns an object containing:
    - audio_url: string (the uploaded file URL for transcription)
    - metadata fields for duration, size, etc.
    
    Export GladiaUploadResponse interface and any error response types. Use standard TypeScript conventions with clear JSDoc comments for each field.
  </action>
  <verify>File exists and exports compile successfully via `npx tsc --noEmit`</verify>
  <done>GladiaUploadResponse interface exists with audio_url field and proper exports</done>
</task>

<task type="auto">
  <name>Task 2: Implement Gladia API Client with File Upload</name>
  <files>src/lib/gladia.ts</files>
  <action>
    Create GladiaClient class with uploadFile method following the research recommendations:
    
    1. Constructor accepts API key parameter
    2. uploadFile method accepts filePath string parameter
    3. File validation pipeline (fs.stat for size, path.extname for format, supported formats from research: mp3, wav, m4a, mp4, mov, avi, flac)
    4. Max file size 1GB (per Gladia limits)
    5. Use Node.js native fetch with FormData - CRITICAL: never set Content-Type header manually
    6. POST to https://api.gladia.io/v2/upload with x-gladia-key header
    7. Parse JSON response and return typed result
    8. Error handling with specific messages for common failures (file not found, too large, wrong format, API errors)
    
    Import required Node.js modules: fs/promises, path, and the type definitions.
  </action>
  <verify>Class compiles and exports properly, file validation logic prevents bad files</verify>
  <done>GladiaClient class with uploadFile method that validates files and uploads via FormData</done>
</task>

</tasks>

<verification>
- GladiaClient constructor accepts API key
- uploadFile method validates file format against supported list
- uploadFile method checks file size against 1GB limit
- FormData upload uses correct multipart format without manual Content-Type header
- Type definitions match expected Gladia API response structure
</verification>

<success_criteria>
- src/lib/gladia.ts exports GladiaClient class with uploadFile method
- src/types/gladia.ts exports GladiaUploadResponse interface 
- File validation rejects unsupported formats and oversized files
- TypeScript compilation passes without errors
- Client ready for MCP tool integration in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-upload/03-01-SUMMARY.md`
</output>